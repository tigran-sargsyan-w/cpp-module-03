# C++ Module 03 – Наследование и «алмазная» проблема 🧬💎

✅ **Статус**: Завершено — все упражнения  
🏫 **Школа**: 42 – C++ Modules (Module 03)  
🏅 **Оценка**: 100/100

> *Одиночное и множественное наследование, переопределение методов, поведение base/derived классов и классическая «diamond problem».*

---

## 📚 Содержание

* [Описание](#-описание)
* [Цели модуля](#-цели-модуля)
* [Обзор упражнений](#-обзор-упражнений)

  * [ex00 – ClapTrap](#ex00--claptrap)
  * [ex01 – ScavTrap](#ex01--scavtrap)
  * [ex02 – FragTrap](#ex02--fragtrap)
  * [ex03 – DiamondTrap](#ex03--diamondtrap)
* [Требования](#-требования)
* [Сборка и запуск](#-сборка-и-запуск)
* [Структура репозитория](#-структура-репозитория)
* [Советы по тестированию](#-советы-по-тестированию)
* [Шпаргалки](#-шпаргалки)

  * [Режимы наследования в C++](#режимы-наследования-в-c--шпаргалка)
  * [Ключевые слова C++ для OOP](#ключевые-слова-c-для-oop-наследование--полиморфизм--шпаргалка)
* [Заметки 42](#-заметки-42)

---

## 📝 Описание

Этот репозиторий содержит мои решения для **C++ Module 03 в школе 42 (C++98)**.

Модуль сфокусирован на **наследовании** и на том, как взаимодействуют базовые и производные классы:

* Одиночное наследование (`ClapTrap` → `ScavTrap` / `FragTrap`)
* Переопределение поведения (например, переопределение `attack()` в наследниках)
* Понимание порядка конструкторов/деструкторов
* Множественное наследование и **«diamond problem»** (`DiamondTrap`)
* Общая базовая часть через **virtual inheritance**

Все упражнения написаны в **C++98** и компилируются со строгими флагами (`-Wall -Wextra -Werror -std=c++98`).

---

## 🎯 Цели модуля

Что прокачивается в этом модуле:

* **Наследование** (`public` наследование для “is-a”)
* **Переопределение** методов и (при необходимости) вызов базовых реализаций
* **Списки инициализации** конструкторов в цепочке наследования
* **Правило трёх** (copy ctor, operator=, destructor)
* **Множественное наследование** и разрешение неоднозначностей
* **Virtual inheritance** как решение «ромба» (одна общая базовая часть)

---

## 📦 Обзор упражнений

### ex00 – ClapTrap

> Небольшой «боевой» класс, который станет базой для остальных упражнений.

**Цель:**
Реализовать `ClapTrap` с каноничным поведением:

* `attack(target)` тратит энергию и печатает действие
* `takeDamage(amount)` уменьшает hit points
* `beRepaired(amount)` тратит энергию и восстанавливает hit points

**Практика:**

* Базовый дизайн класса
* Управление состоянием (HP/EP/AD)
* Copy / assignment / destructor (Rule of Three)

---

### ex01 – ScavTrap

> Первый наследник: специализированное поведение + свои статы.

**Цель:**
Создать `ScavTrap`, наследующийся от `ClapTrap`, с:

* Обновлёнными дефолтными статами (HP/EP/AD по subject)
* Переопределённым `attack()` (сообщение в стиле ScavTrap)
* Новым умением: `guardGate()`

**Практика:**

* `public` наследование и overriding
* Конструкторы наследника, которые корректно вызывают базовый конструктор

---

### ex02 – FragTrap

> Второй наследник: больше урона, больше позитива.

**Цель:**
Создать `FragTrap`, наследующийся от `ClapTrap`, с:

* Обновлёнными дефолтными статами (HP/EP/AD)
* Новым умением: `highFivesGuys()`

**Практика:**

* Ещё больше наследования
* Единый стиль canonical form для всех классов

---

### ex03 – DiamondTrap

> Множественное наследование + «diamond problem».

**Цель:**
Создать `DiamondTrap`, наследующийся и от `FragTrap`, и от `ScavTrap`, при этом:

* Сохранять **одну** общую базу `ClapTrap` (через **virtual inheritance**)
* Иметь своё `name` (личность DiamondTrap)
* Хранить имя базы как `DiamondTrapName_clap_name`
* Использовать `ScavTrap::attack()`
* Добавить `whoAmI()`, который печатает оба имени:

  * имя DiamondTrap
  * имя ClapTrap

**Практика:**

* Множественное наследование
* Разрешение неоднозначностей (`Base::method()`)
* `virtual public Base`, чтобы база была общей и единственной

---

## 🛠 Требования

По subject (типично для C++ модулей 42):

* **Компилятор**: `c++`
* **Флаги**:

  * `-Wall -Wextra -Werror`
  * `-std=c++98`
* **OS**: любая Unix-like система (Linux / macOS)
* **Без внешних библиотек** (никакого C++11+)

---

## ▶️ Сборка и запуск

Клонируй репозиторий и собирай каждое упражнение отдельно.

```bash
git clone <this-repo-url>
cd cpp-module-03
```

### ex00 – ClapTrap

```bash
cd ex00
make
./claptrap
```

### ex01 – ScavTrap

```bash
cd ex01
make
./scavtrap
```

### ex02 – FragTrap

```bash
cd ex02
make
./fragtrap
```

### ex03 – DiamondTrap

```bash
cd ex03
make
./diamondtrap
```

> Названия исполняемых файлов могут отличаться в зависимости от Makefile / реализации.

---

## 📂 Структура репозитория

```text
cpp-module-03/
├── ex00/
│   ├── Makefile
│   ├── ClapTrap.hpp
│   ├── ClapTrap.cpp
│   └── main.cpp
│
├── ex01/
│   ├── Makefile
│   ├── ClapTrap.hpp / ClapTrap.cpp
│   ├── ScavTrap.hpp / ScavTrap.cpp
│   └── main.cpp
│
├── ex02/
│   ├── Makefile
│   ├── ClapTrap.hpp / ClapTrap.cpp
│   ├── FragTrap.hpp / FragTrap.cpp
│   └── main.cpp
│
└── ex03/
    ├── Makefile
    ├── ClapTrap.hpp / ClapTrap.cpp
    ├── ScavTrap.hpp / ScavTrap.cpp
    ├── FragTrap.hpp / FragTrap.cpp
    ├── DiamondTrap.hpp / DiamondTrap.cpp
    └── main.cpp
```

---

## 🔍 Советы по тестированию

### Общее

* Проверь, что **HP/EP/AD** соответствуют дефолтам из subject.
* Убедись, что **энергия может закончиться** и действия прекращаются при EP = 0.
* Убедись, что «мертвые» трапы (HP = 0) не могут атаковать/чиниться.

### ex03 (DiamondTrap)

* Убедись, что конструктор `ClapTrap` вызывается **один раз** (shared base).

* Проверь, что `DiamondTrap::attack()` печатает вывод в стиле **ScavTrap**.

* Проверь, что `whoAmI()` печатает:

  * `DiamondTrap name: Ruby`
  * `ClapTrap name: Ruby_clap_name`

* Порядок деструкторов должен быть логичным (derived → bases, virtual base — последним).

---

## 🧠 Шпаргалки

### Режимы наследования в C++ — шпаргалка

**Ментальная модель:**

* `Human` = базовый класс
* `Warrior` = наследник

---

#### 1) `public` — **“Warrior IS a Human”** ✅

```cpp
class Warrior : public Human {};
```

✅ **Что работает (снаружи):**

```cpp
void greet(Human& h);

Warrior w;
greet(w);        // ✅ OK (is-a)
Human* p = &w;   // ✅ OK
Human& r = w;    // ✅ OK
```

**Когда использовать:**

* классическое OOP **is-a**
* типичный вариант в упражнениях 42 (`ScavTrap` is-a `ClapTrap`)

---

#### 2) `protected` — **“Снаружи: НЕ Human, но для наследников — да”**

```cpp
class Warrior : protected Human {};
```

❌ **Нельзя (внешний код):**

```cpp
void greet(Human& h);

Warrior w;
greet(w);        // ❌ ERROR
Human* p = &w;   // ❌ ERROR
Human& r = w;    // ❌ ERROR
```

✅ **Что можно:**

**1) Использовать `Warrior` как самостоятельный тип**

```cpp
void train(Warrior& w);

Warrior w;
train(w);        // ✅ OK
```

**2) Внутри `Warrior` доступна базовая часть**

```cpp
class Warrior : protected Human
{
public:
    void demo()
    {
        attack();        // ✅ if attack() was public/protected in Human
        hitPoints = 10;  // ✅ if hitPoints was protected in Human
    }
};
```

**3) Внутри наследника от `Warrior` тоже доступна базовая часть**

```cpp
class EliteWarrior : public Warrior
{
public:
    void demo()
    {
        attack();        // ✅ base is visible as protected
        hitPoints = 99;  // ✅ if it was protected in Human
    }
};
```

**Когда использовать:**

* редко
* когда хочешь скрыть “is-a” от внешнего мира, но оставить базу доступной для дальнейших наследников

---

#### 3) `private` — **“Warrior НЕ Human; просто использует Human внутри”** 🔒

```cpp
class Warrior : private Human {};
// For `class`, this is the default:
class Warrior : Human {}; // ❗ private inheritance by default
```

❌ **Нельзя (внешний код):**

```cpp
void greet(Human& h);

Warrior w;
greet(w);        // ❌ ERROR
Human* p = &w;   // ❌ ERROR
Human& r = w;    // ❌ ERROR
```

❌ **Нельзя (даже наследнику от `Warrior`):**

```cpp
class EliteWarrior : public Warrior
{
public:
    void demo()
    {
        attack();        // ❌ ERROR
        hitPoints = 99;  // ❌ ERROR
    }
};
```

✅ **Что можно:**

**1) Внутри `Warrior` базой пользоваться можно**

```cpp
class Warrior : private Human
{
public:
    void demo()
    {
        attack();        // ✅ OK inside Warrior
        hitPoints = 10;  // ✅ if protected in Human
    }
};
```

**2) Выдать наружу только то, что нужно (обёртка / selective re-export)**

```cpp
class Warrior : private Human
{
public:
    using Human::attack; // ✅ make only this base method public

    void specialMove();
};
```

Или прокинуть вручную:

```cpp
class Warrior : private Human
{
public:
    void attackPublic(const std::string& target)
    {
        attack(target);  // ✅ call base inside
    }
};
```

**Когда использовать:**

* редко (часто композиция лучше)
* когда наследование нужно только ради переиспользования кода, а не ради “is-a”

---

**Правило по умолчанию (ВАЖНО) ⚠️**

```cpp
class A : B {};   // = private inheritance (default)
struct A : B {};  // = public inheritance (default)
```

**Супер-коротко:**

* `public`    → **IS-A**
* `protected` → **IS-A только для наследников**
* `private`   → **деталь реализации (переиспользование), не IS-A**

---

### Ключевые слова C++ для OOP (наследование + полиморфизм) — шпаргалка

> Короткий список ключевых слов/приёмов, которые чаще всего важны рядом с наследованием.

---

#### 1) `virtual` (methods) — **polymorphism** 🎭

Если метод базового класса `virtual`, то вызов через `Base&` / `Base*` будет диспатчиться в override наследника.

```cpp
class Base {
public:
    virtual void speak();
};

class Der : public Base {
public:
    void speak() override;
};
```

---

#### 2) `override` — **проверка на этапе компиляции** ✅

```cpp
void speak() override;
```

Если сигнатура не совпала — будет ошибка компиляции.

---

#### 3) `final` — **запретить наследование / запретить дальнейшие overrides** 🧱

```cpp
class Boss final {};
```

```cpp
class Base {
public:
    virtual void speak() final;
};
```

---

#### 4) `virtual` destructor — **безопасный delete через base pointer** 🧨

```cpp
class Base {
public:
    virtual ~Base();
};

class Der : public Base {
public:
    ~Der();
};

Base* p = new Der();
delete p; // ✅ calls ~Der(), then ~Base()
```

Без `virtual ~Base()` есть риск, что вызовется только `~Base()`.

---

#### 5) `protected` — **видно наследникам, скрыто снаружи** 🛡️

```cpp
class Base {
protected:
    int hp;
};
```

---

#### 6) `using Base::method;` — **вернуть перегрузки / выборочно переэкспортировать** 🔎

```cpp
class Base {
public:
    void attack(int);
    void attack(double);
};

class Der : public Base {
public:
    using Base::attack; // ✅ bring base overloads back into scope

    void attack(const char*);
};
```

---

#### 7) `explicit` — **запретить неявные преобразования** 🚫

```cpp
class Money {
public:
    explicit Money(int cents);
};

Money m = 42;   // ❌ not allowed
Money m2(42);   // ✅ allowed
```

---

#### 8) `= delete` / `= default` — **контроль копирования/присваивания** 🧰

```cpp
class Base {
public:
    Base() = default;
    Base(const Base&) = delete;
    Base& operator=(const Base&) = delete;
    virtual ~Base() = default;
};
```

---

#### 9) `Base::method()` — **явный вызов базовой версии** 🎯

```cpp
void Der::attack(const std::string& target)
{
    Base::attack(target); // call base implementation
}
```

---

#### Напоминание: `virtual` в C++ имеет 2 разных смысла ⚠️

* `virtual` **на методах** → полиморфизм (`override` / `final`)
* `virtual` **в наследовании** (`class A : virtual public B`) → одна общая база в «ромбе»

---

## 🧾 Заметки 42

* C++ модули в 42 обычно строго про **C++98** (никаких современных фич, если это не разрешено).
* Norminette для C++ нет, но чистая структура и аккуратные логи очень помогают при оценке.
* Если вывод отличается, проверь:

  * порядок конструкторов/деструкторов
  * какая версия `attack()` вызывается
  * действительно ли база общая в «ромбе» (`virtual public ClapTrap`)

---

Если ты студент 42 и делаешь этот модуль: можешь посмотреть идеи и структуру, но **пиши свою реализацию** — так прогресс будет максимальным. 🚀
